/** The build class
 * Type javac Configure.java to build it
 * Then type `java Configure [OPTIONS]` to create the build script.
 */
class Configure {
    private String prefix, os, build_dir;
    private boolean jar, test, debug;

    Configure(String[] args) {
        
    }
}
